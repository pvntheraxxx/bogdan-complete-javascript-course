/* Колбэк функция */

setTimeout(function myFn() {
    console.log('Привет из функции myFn');
}, 2000);

myFn();

/**
 * 1. setTimeout позволяет через некоторое время
 * вызвать какую-либо другую функцию.
 */