/* Строгий режим */

'use strict'

function myFunction () {
    let a = 2;
    return console.log(a);
};

myFunction();

/* Строгий режим 'use strict' обязует всегда 
объявлять любую переменную, которую мы хотим
использовать. */