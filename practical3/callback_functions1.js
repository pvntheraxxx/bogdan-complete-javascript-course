/* Колбэк функция */

setTimeout(function () {
    console.log('Привет из функции myFn');
}, 1000);


/**
 * setTimeout позволяет через некоторое время
 * вызвать какую-либо другую функцию.
 * 
 * Для вызова колбэк-функции ей не всегда 
 * нужно иметь имя.
*/