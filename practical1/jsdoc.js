/* Комментарий перед функцией */

/**
 * Возвращает разницу двух чисел
 * @param {number} a 
 * @param {number} b 
 * @returns {number} Разница чисел
 */

function diff(a,b) {
    return console.log(`Разница чисел: ${a - b}`); 
}

diff(1,1); // 0
diff(5,2); // 3
diff(10,4); // 6
diff(10,5); // 5

